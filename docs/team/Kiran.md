# Execution PRD — Kiran

## Responsibility
Optimization, decision logic, explainability, baseline/comparison/what-if behavior, and backend/API integration hardening.

## Completed deliverables
- `KIRAN-001` Optimization sizing benchmark + scoring validation; ADR-004 benchmark/evidence. *(Completed & merged — PR #4)*
- `KIRAN-002` Transport-independent deterministic `DecisionPipeline` and domain validation. *(Completed & merged — PR #16)*
- `KIRAN-003` Real FastAPI `/api/optimize` → DecisionPipeline integration hardening. *(Completed & merged — PR #20)*
- `KIRAN-004` Baseline allocation, comparison, explanation, and what-if behavior. Explanation-weight alignment completed through PR #14. *(Completed & merged)*

## KIRAN-003 outcome
The optimization API now exercises the real `DecisionPipeline` rather than duplicating decision logic in the route layer.

Key outcomes:
- `POST /api/optimize` is wired through `DecisionPipeline`.
- Caller-supplied forecast remains supported and is validated against the scenario.
- Forecast is carried in the optimization result for integrated consumers.
- Structured 422 validation is preserved for invalid inputs.
- Global and forecast internal failures return safe structured 500 responses without leaking exception details.
- API integration tests cover Normal/Peak/Surge, deterministic repeatability, baseline-vs-optimized structure, invalid scenarios, forecast compatibility, and safe internal-error handling.

## Validation evidence
- PR #20 merge point: `tests/test_api.py` **40/40 passed**.
- Related pipeline/optimization tests: **71/71 passed**.
- Full suite at that merge point: **220/220 passed**.
- `git diff --check`: clean.
- Latest integrated backend validation after subsequent work: **231/231 passed**.
- Karthi's integration validation: **15/15** determinism checks, hard staff constraints, real-simulator validation, and seed-42 average end-to-end runtime of 13.7 ms Normal / 16.5 ms Peak / 32.0 ms Surge.

## Decision-quality evidence
Existing deterministic demo validation reports **14.1% Normal wait reduction**, **48.7% Peak wait reduction**, and verified Surge capacity-expansion relief. These values must always remain generated by the actual system.

## Current responsibility
The backend/API P0 decision layer is complete. Support Deepansha's dashboard integration and Reethu's final QA only for genuine contract/integration blockers. Do not add backend features or alter algorithms without evidence and team review.

## Critical rules
- Hard resource constraints must always be enforced.
- Explanations must be generated from actual computed metrics and allocation changes.
- Optimization correctness must be validated through the real simulator.
- Deterministic seeded behavior is mandatory.
- No fabricated/cherry-picked/hard-coded metrics and no mock data in the final demo path.
- Keep API contracts synchronized with implementation.

# Scripts

Developer utilities that support the Vortex SICM workflow.

`benchmark_optimizer.py` is used to benchmark the optimization path. Scripts should remain small, reproducible, and non-essential to normal application startup.
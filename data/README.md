# Data

Synthetic workload generation for Vortex SICM.

- `generator.py` creates deterministic synthetic customer-arrival/service data.
- `scenarios.py` defines reusable normal, peak, and surge operating scenarios.

The data layer exists for simulation and demonstration. It must not be populated with real customer information or production bank data.